{
  "name": "pmetra-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Pmetra WASM app — WebSocket + Playwright",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "start:no-browser": "NO_PLAYWRIGHT=1 bun run index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "playwright": "^1.40.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/ws": "^8.18.1"
  }
}
